# CTF scoring smoke test

Throwaway file. This PR exists only to trigger the `ctf-score` workflow and confirm the PR
feedback comments behave correctly. It changes no application code, so it solves zero
challenges — the expected result is the "Scoring complete — no points yet" comment.

Close this PR without merging.
